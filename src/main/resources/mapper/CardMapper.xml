<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="aero.cubox.api.deptemp.mapper.CardMapper">
    <select id="getCardList" resultType="aero.cubox.api.domain.entity.Card">
        SELECT C.*
        FROM T_CARD C
        WHERE C.mdm_dt >= #{lastSyncDt}
          AND C.emp_cd > #{lastSyncEmpCd}
        ORDER BY C.mdm_dt ASC, C.emp_cd ASC
        LIMIT #{pageSize}
    </select>

</mapper>