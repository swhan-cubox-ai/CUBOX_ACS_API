<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="aero.cubox.api.deptemp.mapper.EmpMapper">
    <select id="getEmpList" resultType="aero.cubox.api.sync.vo.EmpVo">
        SELECT E.*, F.feature
        FROM T_EMP E
            LEFT OUTER JOIN T_FACE_FEATURE F ON E.face_id = F.face_id AND F.face_feature_typ = #{featureTyp}
        WHERE E.mdm_dt >= #{lastSyncDt}
          AND E.emp_cd > #{lastSyncEmpCd}
        ORDER BY E.mdm_dt ASC, E.emp_cd
        <if test="pageSize != 0 ">
            LIMIT #{pageSize}
        </if>
    </select>

</mapper>