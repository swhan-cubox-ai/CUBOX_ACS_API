<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="aero.cubox.api.deptemp.mapper.FaceFeatureMapper">


    <select id="find2732Data" resultType="aero.cubox.api.domain.entity.FaceFeature">
        select * FROM T_FACE_FEATURE
        where length(feature_mask) = 2732
        order by id desc
            limit 100
    </select>


</mapper>